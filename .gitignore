# 허용 목록 방식 — 기본은 전부 무시하고, 올릴 것만 되살린다.
# 이 저장소는 공개다. CarrotLabs(구매 에셋 728MB)가 딸려가면 라이선스 위반이 박제된다.
# 무언가를 추가할 때는 반드시 여기에 한 줄을 더하는 방식으로만 한다. `git add -f` 금지.
/*
!/.gitignore
!/CLAUDE.md
!/Wiki/
!/index.html
