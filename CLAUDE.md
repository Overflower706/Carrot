# Carrot

작업 루트. 추론은 영어로 하되 **답변은 한국어로** 한다. 세 저장소 전부 해당한다.

여기에는 세 가지가 있다. Unity 프로젝트는 둘, 그 중 하나 안에 규칙이 하나.

| 정체 | 위치 | 공개 | 상태 |
|---|---|---|---|
| **Carrot Project** | `CarrotProject/` | 비공개 | 액션 RTS. 스팀 출시가 목표 |
| **Control Labs** | `CarrotProject/` 안의 씬 하나 | 빌드만 배포 | 3가지 조작으로 아군 NPC와 함께 적군 NPC를 무찌르는 게임 규칙 |
| **Carrot Labs** | `CarrotLabs/` | 저장소까지 공개 | Control Labs에서 파생된 장난감. 아직 없음 |

**Control Labs**는 저장소가 비공개라 유료 에셋을 그대로 쓴다.
「3가지 조작이 된다」에서 멈추지 않고 **적과 싸우는 구체적 상황 안에서** 조작감을 보는 씬이다.
목적은 인디 게임 개발 씬에 기여하는 것.

**Carrot Labs** 이 목적(인디 게임 개발 씬에 기여)을 확대해서 저장소를 공개까지 한다. 그래서 유료 에셋을 대거 들어내고 형태도 단순해진다.
포맷은 고정이다 — **2D 사이드뷰 + 육각형 타일 오토배틀러 + 플레이어 캐릭터 + 3가지 조작법 + 당신의 규칙.**
하나부터 열까지 고칠 수 있는 자유도는 엔진을 만드는 것과 다르지 않으므로, 고정한 것은 고정으로 둔다.

## 규약 소유권 경계

`CLAUDE.md`는 **cwd에서 루트 방향으로 거슬러 올라가며 전부 읽힌다.**
`CarrotProject/`에서 세션을 시작하면 `CarrotProject/CLAUDE.md`와 **이 파일이 함께** 들어간다.
반대는 아니다 — 루트 세션에 하위 `CLAUDE.md`가 자동으로 들어오지는 않는다(그 폴더의 파일을 건드릴 때 비로소 읽힌다).
**따라서 규약을 어느 파일에 적는지가 곧 「어디까지 적용되는가」다.**

| 이 파일(`Carrot/CLAUDE.md`)이 소유 | 각 프로젝트의 `CLAUDE.md`가 소유 |
|---|---|
| 저장소 경계 · 공개 여부 · 답변 언어 | 그 코드베이스 내부 구조 규약 |
| 커밋 규약 | 그 프로젝트의 위키 문서 배치와 작업 로그 |
| **위키 규약 전체** (바로 아래) | 자기 위키의 고유 사정 — 경로 · 포트 · 제외 폴더 · 자동화 |
| 단계 흐름 관리 | 그 프로젝트 안에서만 성립하는 판단 기준 |

**같은 규칙을 두 곳에 적지 않는다.** 하위 파일은 공통 규약을 다시 쓰는 대신 「공통은 `../CLAUDE.md`」로 가리키고
자기에게만 있는 사정만 적는다. 두 판본이 생기면 세션마다 동작이 갈린다.
(2026-08-18에 위키 태그 규약이 실제로 두 판본으로 갈려 있어서 이 경계를 세웠다.)

`CarrotLabs/` 안에는 **`CLAUDE.md`도 html 문서도 만들지 않는다.**
클로드 작업은 항상 `Carrot/`을 루트로 잡는다. 공개할 때 정돈된 것만 나가게 하기 위해서다.

## 위키 규약 — 세 저장소 공통

위키는 저장소마다 하나씩 `Wiki/`에 있다. **구성 · 스타일 · 편집기 · 태그 체계는 전부 같은 것을 쓴다.**

- `Wiki/index.html` — 문서 목록. **세션 시작 시 여기부터 읽는다.**
- 파일 구성은 `wiki.css`(스타일) · `wiki-edit.js`(편집 UI) · `serve.py` + `wiki.cmd`(로컬 서버).
- 브라우저에서 고치려면 `Wiki/wiki.cmd`를 실행한다.
  **`file://`로 열면 편집 버튼이 뜨지 않는다** — 브라우저가 파일에 쓸 방법이 없어서다.
- **포트는 위키마다 다르다.** 루트 `8765`, `CarrotProject` `8766`.
  같은 포트를 쓰면 나중에 뜬 쪽이 조용히 죽고 브라우저는 먼저 뜬 위키를 보여준다.
- 문서를 새로 만들면 `wiki.css` + `wiki-edit.js`를 걸고 `index.html`에 카드를 추가한다.

**새 html은 뼈대만 잡는다.** `h1`~`h3`로 섹션 제목만 세우고 본문은 비워 둔다.
내용은 사용자가 먼저 직접 적는다. 시키지 않은 문단을 채워 넣지 않는다.

### 섹션 태그 — 축이 둘이다

`h2`(필요하면 `h3`) 제목 끝에 붙는다. 묻는 것이 다르니 헷갈리지 말 것.

| 축 | class | 값 | 누가 붙이나 |
|---|---|---|---|
| 기획이 정해졌는가 | `.status` | `s-fixed` 확정 / `s-open` 미정 / `s-none` 해당 없음 | 문서를 쓰는 쪽 |
| 사람이 읽고 승인했는가 | `.tag` | `t-review` 사람이 문장 검토 필요함 / `t-ok` 사람이 문장 검토함 | **`t-ok`은 사용자만** |

**문구는 `class`가 정한다.** `wiki.css`의 `:empty::after`가 글자를 넣으므로 **빈 `span`만 쓴다.**
class를 바꾸면 글자도 따라 바뀌니 둘이 어긋날 일이 없다.

```html
<h2>제목 <span class="status s-open"></span> <span class="tag t-review"></span></h2>
```

「일부 미정」처럼 표준 문구에 덧붙일 말이 있을 때만 `span` 안에 글자를 적는다.

- **클로드가 어떤 섹션의 문단을 새로 쓰거나 고쳤으면 그 섹션은 다시 `t-review`다.**
- `t-ok`으로 바꾸는 것은 **사용자만** 한다. 클로드가 스스로 달지 않는다.

**진행 상태 배지는 검토 축이 아니다** — `t-now` / `t-next` / `t-later` / `t-block`.
「지금 할 것」「막힘」처럼 상태를 알리는 **자유 문구**라서 이것만 `span` 안에 글자를 적는다
(`<span class="tag t-now">채택</span>`). 검토 태그와 나란히 붙고, 이것이 달렸다고 검토가 끝난 것은 아니다.

### 태그 자동화 — `Wiki/review_tags.py`

**문서를 새로 만들거나 고쳤으면 끝에 돌린다.** 새 섹션이 태그 없이 남지 않게.

```
python Wiki/review_tags.py            # 태그 없는 h2/h3에 t-review를 찍는다 (멱등)
python Wiki/review_tags.py --check    # 안 고치고 현황만 센다
python Wiki/review_tags.py --reset <문서>   # 그 문서의 t-ok을 전부 t-review로 되돌린다
```

- 문서 전체를 손봤으면 `--reset <문서>`를 걸고 사용자가 다시 읽는다.
- 위키마다 사본이 하나씩 있고 **두 사본은 동일하다.** 한쪽을 고치면 다른 쪽에도 같이 옮긴다.
- `index.html`과 `DevLog/` · `Archive/` 폴더는 제외한다 — 목록은 문서가 아니고,
  일지는 그때의 기록이고 아카이브는 폐기된 것이라 지금 와서 승인할 대상이 아니다.
  그런 폴더가 없는 위키에서는 그냥 지나간다.

## 루트 위키 (`Wiki/`)

- `Wiki/Plan.html` — 「조작감 비교 기반 프로젝트 분리 계획」. 무엇을 왜 그 순서로 할지는 전부 거기에 있다.
- `Wiki/wiki.cmd` → <http://127.0.0.1:8765/index.html>. 포트는 `8765`.
- 문서가 평평하다(하위 폴더 없음). `DevLog/`도 `Archive/`도 없다 — 일지는 각 프로젝트 위키가 갖는다.

**이 위키가 다루는 것은 이번 Carrot 프로젝트 전반의 흐름 관리이다.**
Carrot 프로젝트는 Carrot Project 게임 출시를 목표로, Control Labs 빌드 공개 및 공유 - Carrot Labs 저장소 커뮤니티에 공유 - Carrot Project 게임 출시라는 단계를 밟는다.
이 단계를 생각하며 현재의 작업 상황과 앞으로의 필요한 작업들을 바깥에서 관리한다.
CarrotProject 내부의 기획·구현 문서는 그 프로젝트 자신의 `Wiki/`가 소유한다. 여기로 옮겨오지 않는다.
다만 Carrot 안에 하위 프로젝트 폴더 구조가 드러나 있어 연결이 보장되므로, 이 위키의 html 문서에서
하위 프로젝트(`CarrotProject/`, `CarrotLabs/`) 문서로 링크를 거는 것은 허용한다. 소유권만 옮기지 않을 뿐이다.
한 편 필요시 하위 클로드 에이전트들이 자신의 현황을 기록하는 게시판의 역할도 겸한다.
Labs의 경우에는 개발 중에는 위키를 담지 않기에 여기서 다룬다.

## 커밋

**커밋 메시지에 `Claude-Session:` 트레일러를 붙이지 않는다.** 세 저장소 전부 해당한다.
루트가 공개라 세션 URL이 히스토리에 그대로 박히고, 나중에 지우려면 히스토리를 다시 써야 한다.
(2026-08-13에 `Carrot` 6커밋 · `CarrotProject` 39커밋에서 걷어냈다.)

**저장소가 셋이고 각각 `.git`을 가진다.** `git`은 cwd가 속한 저장소를 때린다 —
루트에서 커밋하면 **공개 저장소**다. 루트 `.gitignore`가 허용 목록이라
`CarrotProject/` 파일을 고친 뒤 루트에서 커밋하면 조용히 아무 일도 일어나지 않는다.
**그 프로젝트의 변경은 그 프로젝트 폴더에서 커밋한다.**

### 공개 Pages — 한시적

<https://overflower706.github.io/Carrot/> (repo `Overflower706/Carrot`, **public**).
2026-08-13에 `labs-wiki`에서 개명했다 — 폴더 이름과 저장소 이름을 맞췄다. 옛 URL은 GitHub가 리다이렉트해 준다.
`main`에 push하면 1~2분 뒤 반영된다.

- **이 저장소는 공개다.** `.gitignore`가 허용 목록 방식인 이유가 그것이다 —
  옆의 `CarrotProject`에는 구매 에셋이 728MB 있고, 한 번 딸려가면 되돌릴 수 없다.
  **`git add -f`를 쓰지 말 것.** 올릴 것이 생기면 `.gitignore`에 한 줄을 더한다.
- **이 파일도 공개로 나간다**(`.gitignore`의 `!/CLAUDE.md`). 비공개 프로젝트 내부 사정을 여기에 적지 않는다.
- `Wiki/index.html`의 `../CarrotProject/...` 링크는 **웹에서 404다.** 로컬 전용이고, 그래야 맞다.
- **분리 계획이 끝나면 이 Pages는 내린다.** 목적이 「계획이 굴러가는 동안 폰에서 읽는 것」뿐이라
  끝난 뒤에도 공개로 두면 이유 없이 노출만 남는다.

## CarrotProject 를 건드릴 때

`CarrotProject/CLAUDE.md`가 그 프로젝트의 규약이다. **반드시 먼저 읽는다.**
분리 작업은 원본을 건드리지 않는다 — 원본은 액션 RTS를 그대로 계속한다.

## 공개 저장소를 만들 때

`Wiki/Plan.html` 4장 순서를 따른다. 저장소는 `Overflower706/CarrotLabs`로 **이미 팠다(2026-08-13, 비공개)**.
골격 커밋 하나가 올라가 있고, `Wiki/Plan.html` 3장이 전부 끝난 뒤에 공개로 돌린다.

공개 전 필수 (`Wiki/Plan.html` 3장) — 하나라도 남으면 라이선스 위반이 공개된 채로 박제된다:

1. 구매 에셋(Layer Lab, Lana Studio, CelerisLab, OccaSoftware, DOTween) **0개**
2. `Packages/manifest.json`의 로컬 패키지 `com.ovfl.utilities` 제거
3. 캐릭터는 캡슐/사각형 도형으로 새로 만든다 (비교에는 그림이 방해다)
4. 히스토리 세탁은 **하지 않는다.** 새로 파고 필요한 것만 옮긴다.

## 원본과의 방향

조작 스크립트가 갈라지면 **`CarrotLabs`(공개)가 원본**, `CarrotProject`가 복사본이다.
개선은 항상 **공개 → 비공개** 방향으로만 흐른다. 반대로 하면 옮길 때마다 다시 세탁해야 한다.
